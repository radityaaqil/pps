const userHandler = require("./user");
const programHandler = require("./program");
const assignationHandler = require("./assignation");

module.exports = {
  userHandler,
  programHandler,
  assignationHandler,
};
